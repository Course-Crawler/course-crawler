services:
  video-recorder1:
    container_name: video-recorder1
    environment:
      - EMAIL=${EMAIL}
      - PASSWORD=${PASSWORD}
      - VIDEO_TO_RECORD_ID=3
      - CHUNK_SIZE=1
    extends:
      service: video-recorder
      file: common-services.yaml
  video-recorder2:
    container_name: video-recorder2
    environment:
      - EMAIL=${EMAIL}
      - PASSWORD=${PASSWORD}
      - VIDEO_TO_RECORD_ID=14
      - CHUNK_SIZE=2
    extends:
      service: video-recorder
      file: common-services.yaml
  video-recorder3:
    container_name: video-recorder3
    environment:
      - EMAIL=${EMAIL}
      - PASSWORD=${PASSWORD}
      - VIDEO_TO_RECORD_ID=22
      - CHUNK_SIZE=2
    extends:
      service: video-recorder
      file: common-services.yaml
